<h1>Book#index</h1>
<% if @books.any? %>
  <% @books.each do |book| %>
    Name: <%= book.name %> &nbsp &nbsp <%= link_to 'View', book_view_path(:id => book.id) %> <br>
  <% end %>
  <% else %>
  Nothing here now.
<% end %>
