<h1>Book#index</h1>
<%# link_to 'Create Book', book_new_path() %>
<%= link_to 'See your own records', book_current_path %>
<br>
<% if @books.any? %>
  <% @books.each do |book| %>
    Name: <%= book.name %> &nbsp &nbsp <%= link_to 'View', book_view_path(:id => book.id) %> <br>
  <% end %>
  <% else %>

  Nothing here now.
<% end %>
<br>
<br>
<br>
<%= will_paginate @books,previous_label:"Previous", next_label:"Next", inner_window: 1, outer_window: -2, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
