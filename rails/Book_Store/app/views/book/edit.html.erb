<h1>Book#edit</h1>
<%= form_for @book,url: book_update_path(:id => @book.id), method: :patch  do |f| %>
  <p>Name:</p>
  <%= f.text_field :name %>
  <p>Price</p>
  <%= f.number_field :price %>

  <%= f.hidden_field :user_id, :value => current_user.id %>

  <%= f.submit "Submit" %>
<% end %>
